from distutils.core import setup
from setuptools import tools

setup(name='myexamplepkg',
      version='1.0',
      
      # list folders, not files
      packages=['myexamplepkg'], # Include packages in the project)
